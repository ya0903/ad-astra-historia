node_modules/
dist/
.env
config.json
saves/
.superpowers/

# Downloaded Natural Earth GeoJSON (auto-downloaded by server on startup)
# These are large files fetched from github.com/nvkelso/natural-earth-vector
# Do NOT ignore them — they must be committed so deploys work without outbound internet
